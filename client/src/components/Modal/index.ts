import { ModalRoot as Root } from './modal-root';
import { ModalPortal as Portal } from './modal-portal';
import { ModalContent as Content } from './modal-content';
import { ModalCloseButton as CloseButton } from './modal-close-button';

export const Modal = { Root, Portal, Content, CloseButton };
